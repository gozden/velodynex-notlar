projection;
strict ( 2 );
use draft;

define behavior for ZVDX_C_NOT alias Notlar
use etag
{
  use create;
  use update;
  use delete;
  use action tamamla;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}